いつも<%= OwnerHelper::Application::SERVICE_NAME %>をご利用いただきありがとうございます。

<%= @resident.name %>から新着メッセージが届きました
===============================================
 
<%= @chat.text[0..100] + "..." %>

下記リンクよりメッセージを確認してください。

<%= edit_owner_contact_url(id: @chat.contact_id) %>
